import { StyleSheet, Text, View } from 'react-native'
const styles = StyleSheet.create({
    container:{
        backgroundColor:'#fff',
        minHeight:400,
        width:320,
        borderRadius:20
    }
})

export default styles
